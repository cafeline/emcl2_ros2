# 作業記録: EMCL2 3D圧縮地図対応リファクタリング詳細

## 0. 最新更新（RawVoxelGridMap 版）
- pointcloud_compressor が出力する `/raw_voxel_grid` HDF5 を直接扱う `RawVoxelGridMap` を新設し、圧縮辞書ベースの `CompressedVoxelMap` 実装と関連テスト／ビルド項目を削除。
- センサモデル／Particle／Mcl／EMcl2Node は新マップ API（0〜255 の占有値を閾値なしで判定）に移行済み。
- ユニットテストは raw HDF5 サンプルを生成するユーティリティへ置き換え、`colcon test --packages-select emcl2` で lint を含む全テストが成功することを確認。
- 以下の旧節は圧縮辞書版の記録として残している。

## 1. 全体概要
- 目的: pointcloud_compressor が生成する HDF5 圧縮地図を EMCL2 ノードが直接読み込み、`sensor_msgs/PointCloud2` を観測として利用しながら XY-Yaw の自己位置推定を行う構成へ刷新する。
- 方針: 既存の LaserScan / 2D 占有格子ベースのコードを撤去し、3D 圧縮ボクセル辞書を常駐させた判定 API と、それに最適化された粒子フィルタ（MCL）を再構築する。
- 成果: 地図ローダ・センサモデル・粒子更新・ROS2 ノード骨格・ユニットテストを TDD で整備し、`colcon test` で lint を含む全テストをパス。

## 2. ビルド構成と依存関係の更新
- `CMakeLists.txt`
  - Eigen3 / HDF5 (C, HL) を明示的に `find_package`。圧縮地図ローダが HDF5 C API を直接利用するため必須。
  - 主要ソースを `emcl2_core` 共有ライブラリにまとめ、ノード (`src/main.cpp`) はこれをリンクする軽量エントリポイントへ変更。
  - GTest を利用した 4 本のユニットテスト（CompressedVoxelMap, SensorModel, Particle, Mcl）を `ament_add_gtest` で登録し、依存ライブラリをリンク。
  - 旧 LaserScan 系ソース (`ExpResetMcl2.cpp`, `CompressedMap.cpp` など) は削除対象となるためビルドリストから排除済み。

## 3. HDF5 圧縮地図ローダ (`CompressedVoxelMap`)
- ヘッダ: `include/emcl2/CompressedVoxelMap.h`
  - `voxel_size_`, `block_size_`, `origin_`（地図原点）、`pattern_length_`, `pattern_bytes_`、辞書バイト列、およびブロック座標→辞書パターン index の `unordered_map` を保持。
  - `BlockCoord` / `BlockCoordHash` を定義し、3D グリッドブロック単位でアクセす可能とした。

- 実装: `src/CompressedVoxelMap.cpp`
  - HDF5 C API を利用し、以下の順序で読み込み。
    1. `/compression_params` グループの `voxel_size`, `block_size`, `pattern_bits`, `grid_origin` を検証付きで取得。
    2. `/dictionary` グループから `pattern_length`, `patterns`（辞書パターン連結バイト列）をロード。
    3. `/compressed_data` グループから `indices`（各ブロックの辞書 index）と `voxel_positions`（ブロック位置 3 要素）を読み込み、`unordered_map` に格納。
  - 例外安全: 読み込み中に例外が発生した場合は内部状態を初期化し false を返す。
  - `isOccupied(...)`:
    - 世界座標から `origin_` を減算しボクセルインデックスへ変換。
    - ブロック座標を算出し、対応する辞書 index を検索。
    - ブロック内ローカル座標からビット位置（`pattern_bytes_`）を求め、辞書パターン配列の該当ビットをチェック。
    - 該当ビットが 1 の場合に占有とみなす。
  - `voxelSize()` はパラメータ確認用に提供。

## 4. 観測表現とセンサモデル
- 観測構造体: `include/emcl2/PointCloudObservation.h`
  - `points`: Eigen::Vector3d の列で点群を格納。
   - `sensor_offset`: センサ原点（基準フレームからの平行移動）。
  - `sensor_rotation`: センサ姿勢（Quaternion）。

- 尤度計算: `src/SensorModel.cpp`
  - 粒子姿勢 `Pose` の Yaw のみを使用して 2D 回転行列の cos/sin を事前計算。
  - 観測点をセンサ姿勢で回す → オフセット平行移動 → 粒子姿勢で map 座標へ投影。
  - `CompressedVoxelMap::isOccupied()` で占有判定し、ヒット数を重み（score）として返却。
  - 生点群を使用し、フィルタや確率密度関数は導入しない（ヒットカウント方式）。

## 5. 粒子・MCL 再実装
- 粒子 (`include/emcl2/Particle.h`, `src/Particle.cpp`)
  - `Pose` + `weight` を保持。
  - `updateWeight()` は SensorModel を呼び出して結果をそのまま重みに反映。

- MCL 本体 (`include/emcl2/Mcl.h`, `src/Mcl.cpp`)
  - `sensorUpdate()` で全粒子に対し `Particle::updateWeight()` を実行。
  - `normalizeWeights()` は合計重みが 0 以下の場合は何もしない（全粒子 0 の場合は後続処理に委ねる）。
  - `resample()` は低分散サンプリング（systematic resampling）。
    - 重み総和が 0 以下の場合は一様重みで早期リターン。
    - `std::uniform_real_distribution` と `step` を利用し、累積和から粒子を選択。
    - リサンプリング後は全粒子の重みを一様に再設定。

- オドメトリモデル (`include/emcl2/OdomModel.h`, `src/OdomModel.cpp`)
  - `setDev(length, angle)` で移動距離と回転角の寄与から前進／回転ノイズ標準偏差を算出。
  - `drawFwNoise()`, `drawRotNoise()` は正規分布サンプルを返す。
  - 既存コードを継承しつつ、新粒子フィルタでも利用可能な構造。

## 6. ROS2 ノード (`EMcl2Node`)
- 定義: `include/emcl2/emcl2_node.h`
  - 新しいメンバ構成: HDF5 地図 (`CompressedVoxelMap`), 粒子フィルタ (`Mcl`), オドメトリモデル, 観測テンプレート, TF リソース、パラメータ類（frame 名, topic 名, 初期姿勢, 粒子数, センサ姿勢, オドメトリノイズなど）。

- 実装: `src/emcl2_node.cpp`
  - **起動シーケンス**
    1. `declareParameter()` で必要パラメータを宣言＆取得。
    2. `loadMap()` で HDF5 ファイルを一度だけ読み込み。空／失敗時は `InvalidParametersException` を throw。
    3. `initializeParticles()` で初期粒子群をガウス分布から生成し、一様重み化。
    4. `initTF()` で tf2 Buffer/Listener/Broadcaster をセットアップ。
    5. `initCommunication()` で PointCloud2 サブスクライバ、Pose / ParticleCloud パブリッシャを初期化。
    6. `create_wall_timer()`（`odom_freq_`>0 の場合）で周期的な `loop()` を実行。

  - **PointCloud2 コールバック** (`pointCloudCallback`)
    - 取得時に `updateWithOdometry()` で最新オドメトリを反映。
    - `sensor_msgs::PointCloud2Iterator` で x/y/z を読み出し、NaN/Inf のみ除外して観測点配列を構築。
    - 観測点数が 0 の場合は何もしない。
    - PF に対し `sensorUpdate()` → `normalizeWeights()` → `resample()` を順に実行。
    - リサンプリング後、メッセージ `stamp` を用いて `publishOutputs()`。

  - **オドメトリ適用** (`updateWithOdometry`)
    - `tf_buffer_->lookupTransform(odom_frame, base_frame, TimePointZero)` で最新 TF を取得。
    - 初回は `last_odom_pose_` を記録し終了。
    - 2 回目以降は差分 Pose を計算し、近似ゼロなら終了。
    - 距離・方向を算出し、`odom_model_->setDev()` でノイズ分散更新。
    - 粒子ごとに `pose().move(length, direction, delta_yaw, fw_noise, rot_noise)` を適用。

  - **出力** (`publishOutputs`)
    - `computeWeightedMean()` で平均姿勢と分散 (x/y/yaw) を算出。Yaw は角度の平均を sin/cos で求め、2πラップアラウンド補正あり。
    - `buildPoseMessage()` で `geometry_msgs::msg::PoseWithCovarianceStamped` を構築し `pose_pub_` へ送信。
    - `buildParticleArray()` で `geometry_msgs::msg::PoseArray` を作成し `particle_pub_` へ送信。
    - map→odom TF: `odom->base` の現在値を取得し、推定 pose に基づく map→base を組み合わせて算出。失敗時は単位クォータニオンを仮定。
    - `tf_broadcaster_->sendTransform()` でブロードキャスト。

  - **その他**
    - `loop()` は timer から呼ばれ、オドメトリ更新＋最新時刻での出力を行う。
    - `getOdomFreq()` は `main.cpp` のループ周期算出に使用。

## 7. エントリポイント (`src/main.cpp`)
- `rclcpp::Rate` を利用したメインループを最小限に保持。
- ループ内で `node->loop()` → `rclcpp::spin_some(node)` → `sleep` の順に処理、既存仕様を踏襲しつつ新ノード実装に連携。

## 8. テスト基盤
- HDF5 テストユーティリティ: `test/hdf5_test_utils.hpp`
  - 参照: HDF5 ファイルを一時ディレクトリへ生成するヘルパ群 (`write_scalar_*`, `write_array_*`, `write_matrix_int32`)。
  - `create_basic_hdf5_map()` は 2×2×2 ブロックのシンプルな地図を生成し、辞書パターンを 1 つだけ持つ最小ケースを提供。

- 各ユニットテスト
  1. `test/test_compressed_voxel_map.cpp`: 読み込み成功と占有ヒット/ミスを検証。
  2. `test/test_sensor_model.cpp`: センサ姿勢・Yaw 回転を含むヒット数評価を 3 ケースで確認。
  3. `test/test_particle.cpp`: 粒子重みがヒットで 1、ミスで 0 になることを確認。
  4. `test/test_mcl.cpp`: センサ更新で整合粒子の重みが大きくなること、およびリサンプリング後の重みが一様になることを確認。

- `colcon test --packages-select emcl2 --event-handlers console_direct+`
  - cppcheck / flake8 / lint_cmake / pep257 / uncrustify / xmllint の lint 系テストも PASS。
  - 初回実行で uncrustify 差分が検出されたため `ament_uncrustify --reformat` を実行し、2 回目のテストで全項目 PASS。

## 9. 削除／非対応要素
- レーザースキャン系クラス群 (`CompressedMap`, `LikelihoodFieldMap`, `ExpResetMcl2`, `Scan` 等) はすべて削除。
- 2D Map 更新や再生成に関わるロジック・キャッシュ機構を除去。
- 点群のダウンサンプリングやフィルタリング処理は意図的に実装しない方針。
- 地図の再読み込みや更新機能は提供せず、起動時に読み込んだ辞書データを常駐させる。

## 10. 今後の確認／調整課題
- 実ロボット / 実データと TF 構成を合わせ、PointCloud2 のフレーム ID が map と整合することを再確認する必要あり。
- 粒子数・オドメトリノイズ・初期分散などパラメータは実環境でチューニングが必要。
- 高密度点群でのヒットカウント効率を確保するため、必要に応じて C++ SIMD や並列化を検討する余地あり（現状は単純ループ）。
- センサ・オドメトリ異常時の例外ログは導入済みだが、リカバリ機構（リセット等）は未実装のため運用側での対応を想定。

以上。
