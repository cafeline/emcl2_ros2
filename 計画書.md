# EMCL2 3D圧縮地図対応計画書

## 1. 概要
- 目的: VoxelHashing 系ツールが生成する 3D ハッシュ地図 (`.npz`) を EMCL2 が直接利用し、`x, y, yaw` の自己位置推定を行う。
- 入力: `sensor_msgs/PointCloud2` (フィルタ無しの生データ) と TF。
- 出力: 既存の MCL 出力 (姿勢、particlecloud 等)。
- 制約: 地図は起動時に一度だけ読み込み、更新なし。メモリ余裕あり。

## 2. システム構成
- **データフロー**
  - 起動時: NPZ ファイル（VoxelHashing 形式）を読み込み → ハッシュエントリと SDF ブロックをメモリ展開。
  - 実行時: PointCloud2 購読 → 粒子姿勢で点群を世界座標へ変換 → 地図占有判定 → 粒子尤度更新。
- **主要コンポーネント**
  - `HashedVoxelMap` (新規): VoxelHashing 互換ハッシュを保持し、占有判定 API を提供。
  - `EMcl2Node`: LaserScan 依存排除、PointCloud2 読み込み、PF 呼び出し。
  - `ExpResetMcl2` ほか PF クラス: 3D占有判定 API を利用するよう更新。

## 3. 要件整理
1. 地図入力
   - HDF5 ファイルパスをパラメータ指定。
   - `PatternDictionary` 相当の情報を全展開 (オンデマンド展開不要)。
2. センサ入力
   - `PointCloud2` を購読し、生データを使用。
   - LaserScan 関連コードを削除。
3. 尤度計算
   - 点が占有ボクセルか否かのみで判定 (追加フィルタ無し)。
   - 粒子状態は `(x, y, yaw)` に限定。`z` は固定、`roll/pitch` も固定値。
4. メンテナンス
   - 地図は読み取り専用。再読込や更新機能不要。
   - キャッシュ制限などのメモリ最適化不要。

## 4. 設計方針
- **地図読み込み**
  - `cnpy` 相当のローダで `.npz` 内の `hash_entries_*`, `sdf_blocks`, `voxel_size` 等を取得。
  - ハッシュエントリを復元し、SDF ブロックの weight を `std::vector<uint8_t>` として保持。
- **座標変換**
  - TF で LiDAR → base → map を解決 (既存 TF インフラを再利用)。
  - 粒子ごとに センサ原点を `(x_p, y_p, z_fixed)` に設定し yaw 回転。
- **占有判定 API**
  - `bool isOccupied(float wx, float wy, float wz)` : グローバル座標をボクセルインデックスへ変換し、格納済みテーブルを参照。
- **PF 連携**
  - MCL 各クラスは `HashedVoxelMap` を受け取り、PointCloud2 ベースの尤度計算を行う。
  - 旧 `CompressedVoxelMap` 関連コードと HDF5 依存を廃止。

## 5. 実装タスク
1. **インフラ整備**
   - [ ] パラメータ定義 (`map_voxelhash_path`, `pointcloud_topic`, 固定 `sensor_z`, `sensor_roll`, `sensor_pitch` 等)。
   - [ ] 旧 LaserScan / HDF5 関連コード・パラメータの削除。
2. **地図ローダ**
   - [ ] `HashedVoxelMap` クラス作成 (ヘッダ/実装/テスト)。
   - [ ] `.npz` 読み込み処理を実装し、ハッシュエントリと SDF ブロックを復元。
3. **PointCloud2 対応**
   - [ ] `EMcl2Node` の PointCloud2 サブスクライバを整理し、パラメータ連携を更新。
   - [ ] 点群→世界座標変換ユーティリティを整備。
4. **PF 更新**
   - [ ] `Mcl` / `Particle` / `SensorModel` が `HashedVoxelMap` を利用するよう更新。
   - [ ] 初期化/リセット処理が新マップクラスを利用するよう調整。
5. **バリデーション**
   - [ ] ユニットテスト (NPZ ロード、占有判定境界、MCL の動作)。
   - [ ] bag などを用いた統合試験。

## 6. 確認項目
- NPZ 読み込み失敗時のエラー処理。
- TF 取得失敗時のリトライ/ログ。
- PointCloud2 フレーム ID とマップフレームの整合。
- 粒子重みが極端に０／飽和しないか (閾値調整)。
- 既存トピック・サービス (particlecloud, mcl_pose, global_localization) が維持されること。

## 7. リスクと対応
- **VoxelHashing 出力差異**: 想定外のデータ欠損 → 読み込み時にバリデーションを追加。
- **点群密度の影響**: 高密度で尤度計算が重くなる → 必要に応じ粒子数や点群割合をパラメータ化 (ただし初期実装では変更しない)。
- **TF 設定不足**: map/base/センサ間TFが揃わず推定不可 → 使用手順書で明記し、ログで検出。

## 8. スケジュール案
1. 設計/骨組み実装: 2日
2. 地図ローダ＋PF改修: 3日
3. テスト整備・統合試験: 2日
4. 調整・ドキュメント整備: 1日

合計 8日 (稼働ベース)。
