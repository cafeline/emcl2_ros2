# EMCL2 Octomap 対応計画書

## 1. 概要
- 目的: Octomap が生成する 3D 占有地図 (`.bt` / `.ot`) を EMCL2 が直接利用し、`x, y, yaw` の自己位置推定を行う。
- 入力: `sensor_msgs/PointCloud2` (フィルタ無しの生データ) と TF。
- 出力: 既存の MCL 出力 (姿勢、particlecloud 等)。
- 制約: 地図は起動時に一度だけ読み込み、更新なし。メモリ余裕あり。

## 2. システム構成
- **データフロー**
  - 起動時: Octomap ファイル読み込み → `octomap::OcTree` を構築しメモリ常駐。
  - 実行時: PointCloud2 購読 → 粒子姿勢で点群を世界座標へ変換 → Octomap で占有判定 → 粒子尤度更新。
- **主要コンポーネント**
  - `OctomapMap`: Octomap ファイル読み込み・占有判定 API。
  - `EMcl2Node`: LaserScan 依存排除、PointCloud2 読み込み、PF 呼び出し。
  - 既存 PF クラス: Octomap 経由の占有判定 API を利用するよう更新。

## 3. 要件整理
1. 地図入力
   - Octomap ファイルパス (`octomap_path`) をパラメータ指定。
   - `.bt`(binary) / `.ot`(text) 双方に対応。読み込み後は読み取り専用。
2. センサ入力
   - `PointCloud2` を購読し、生データを使用。
   - LaserScan 関連コードを削除。
3. 尤度計算
   - Octomap 上で点が占有セルか否かのみで判定 (追加フィルタ無し)。
   - 粒子状態は `(x, y, yaw)` に限定。`z` は固定、`roll/pitch` も固定値。
4. メンテナンス
   - 地図は読み取り専用。再読込や更新機能不要。
   - キャッシュ制限などのメモリ最適化不要。

## 4. 設計方針
- **地図読み込み**
  - Octomap 提供の `octomap::AbstractOcTree::read()` / `octomap::OcTree::readBinary()` を利用し、`OcTree` オブジェクトとして保持。
- **座標変換**
  - TF で LiDAR → base → map を解決 (既存 TF インフラを再利用)。
  - 粒子ごとに センサ原点を `(x_p, y_p, z_fixed)` に設定し yaw 回転。
- **占有判定 API**
  - `bool isOccupied(double wx, double wy, double wz)` : Octomap の `search` / `isNodeOccupied` をラップ。
- **PF 連携**
  - `ExpResetMcl2::sensorUpdate` を PointCloud2 点列対応に変更。
  - `CompressedMap` クラスの置換/廃止。

## 5. 実装タスク
1. **インフラ整備**
   - [ ] パラメータ定義 (`map_hdf5_path`, `pointcloud_topic`, 固定 `sensor_z`, `sensor_roll`, `sensor_pitch` 等)。
   - [ ] 旧 LaserScan 関連コード・パラメータの削除。
2. **地図ローダ**
   - [ ] `OctomapMap` クラス作成 (ヘッダ/実装/テスト)。
   - [ ] Octomap 読み込み処理を実装し `OcTree` を保持。
3. **PointCloud2 対応**
   - [ ] `EMcl2Node` に PointCloud2 サブスクライバを追加。
   - [ ] 点群→世界座標変換ユーティリティ実装。
4. **PF 更新**
   - [ ] PF クラスの尤度計算を Octomap 占有判定に置換。
   - [ ] 初期化/リセット処理が新マップクラスを利用するよう更新。
5. **バリデーション**
   - [ ] ユニットテスト (地図読み込み・占有判定境界・変換計算)。
   - [ ] bag などを用いた統合試験。

## 6. 確認項目
- Octomap 読み込み失敗時のエラー処理。
- TF 取得失敗時のリトライ/ログ。
- PointCloud2 フレーム ID とマップフレームの整合。
- 粒子重みが極端に０／飽和しないか (閾値調整)。
- 既存トピック・サービス (particlecloud, mcl_pose, global_localization) が維持されること。

## 7. リスクと対応
- **Octomap ファイル不整合**: 拡張子やツリー種別が異なる場合に備え、読み込み時に型チェックとログ出力を行う。
- **点群密度の影響**: 高密度で尤度計算が重くなる → 必要に応じ粒子数や点群割合をパラメータ化 (ただし初期実装では変更しない)。
- **TF 設定不足**: map/base/センサ間TFが揃わず推定不可 → 使用手順書で明記し、ログで検出。

## 8. スケジュール案
1. 設計/骨組み実装: 2日
2. 地図ローダ＋PF改修: 3日
3. テスト整備・統合試験: 2日
4. 調整・ドキュメント整備: 1日

合計 8日 (稼働ベース)。
