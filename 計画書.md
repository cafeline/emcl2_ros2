# EMCL2 3D圧縮地図対応計画書

> **更新メモ（2025-10-12）**  
> 実装は `/raw_voxel_grid` を対象とする `RawVoxelGridMap` ベースへ移行済み。以下は圧縮辞書版の初期計画として残している。

## 1. 概要
- 目的: pointcloud_compressor が生成する3D圧縮地図 (PatternDictionary/HDF5) を EMCL2 が直接利用し、`x, y, yaw` の自己位置推定を行う。
- 入力: `sensor_msgs/PointCloud2` (フィルタ無しの生データ) と TF。
- 出力: 既存の MCL 出力 (姿勢、particlecloud 等)。
- 制約: 地図は起動時に一度だけ読み込み、更新なし。メモリ余裕あり。

## 2. システム構成
- **データフロー**
  - 起動時: HDF5 ファイル読み込み → 圧縮辞書とブロックインデックスをメモリ展開。
  - 実行時: PointCloud2 購読 → 粒子姿勢で点群を世界座標へ変換 → 地図占有判定 → 粒子尤度更新。
- **主要コンポーネント**
  - `CompressedVoxelMap` (新規): 圧縮パターン保持と占有判定 API。
  - `EMcl2Node`: LaserScan 依存排除、PointCloud2 読み込み、PF 呼び出し。
  - `ExpResetMcl2` ほか PF クラス: 3D占有判定 API を利用するよう更新。

## 3. 要件整理
1. 地図入力
   - HDF5 ファイルパスをパラメータ指定。
   - `PatternDictionary` 相当の情報を全展開 (オンデマンド展開不要)。
2. センサ入力
   - `PointCloud2` を購読し、生データを使用。
   - LaserScan 関連コードを削除。
3. 尤度計算
   - 点が占有ボクセルか否かのみで判定 (追加フィルタ無し)。
   - 粒子状態は `(x, y, yaw)` に限定。`z` は固定、`roll/pitch` も固定値。
4. メンテナンス
   - 地図は読み取り専用。再読込や更新機能不要。
   - キャッシュ制限などのメモリ最適化不要。

## 4. 設計方針
- **地図読み込み**
  - `pointcloud_compressor::HDF5IO` ベースで `compression_params`, `dictionary`, `compressed_data` を取得。
  - ブロック毎にビット列を解凍し、占有ボクセルを `std::vector<uint8_t>` 等で保持。
- **座標変換**
  - TF で LiDAR → base → map を解決 (既存 TF インフラを再利用)。
  - 粒子ごとに センサ原点を `(x_p, y_p, z_fixed)` に設定し yaw 回転。
- **占有判定 API**
  - `bool isOccupied(float wx, float wy, float wz)` : グローバル座標をボクセルインデックスへ変換し、格納済みテーブルを参照。
- **PF 連携**
  - `ExpResetMcl2::sensorUpdate` を PointCloud2 点列対応に変更。
  - `CompressedMap` クラスの置換/廃止。

## 5. 実装タスク
1. **インフラ整備**
   - [ ] パラメータ定義 (`map_hdf5_path`, `pointcloud_topic`, 固定 `sensor_z`, `sensor_roll`, `sensor_pitch` 等)。
   - [ ] 旧 LaserScan 関連コード・パラメータの削除。
2. **地図ローダ**
   - [ ] `CompressedVoxelMap` クラス作成 (ヘッダ/実装/テスト)。
   - [ ] HDF5 読み込み処理を実装して一括展開。
3. **PointCloud2 対応**
   - [ ] `EMcl2Node` に PointCloud2 サブスクライバを追加。
   - [ ] 点群→世界座標変換ユーティリティ実装。
4. **PF 更新**
   - [ ] `ExpResetMcl2` の尤度計算を 3D占有判定に置換。
   - [ ] 初期化/リセット処理が新マップクラスを利用するよう更新。
5. **バリデーション**
   - [ ] ユニットテスト (地図読み込み・占有判定境界・変換計算)。
   - [ ] bag などを用いた統合試験。

## 6. 確認項目
- HDF5 読み込み失敗時のエラー処理。
- TF 取得失敗時のリトライ/ログ。
- PointCloud2 フレーム ID とマップフレームの整合。
- 粒子重みが極端に０／飽和しないか (閾値調整)。
- 既存トピック・サービス (particlecloud, mcl_pose, global_localization) が維持されること。

## 7. リスクと対応
- **HDF5 フォーマット差異**: 想定外のデータ欠損 → 読み込み時にバリデーションを追加。
- **点群密度の影響**: 高密度で尤度計算が重くなる → 必要に応じ粒子数や点群割合をパラメータ化 (ただし初期実装では変更しない)。
- **TF 設定不足**: map/base/センサ間TFが揃わず推定不可 → 使用手順書で明記し、ログで検出。

## 8. スケジュール案
1. 設計/骨組み実装: 2日
2. 地図ローダ＋PF改修: 3日
3. テスト整備・統合試験: 2日
4. 調整・ドキュメント整備: 1日

合計 8日 (稼働ベース)。
